# Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
streamlit>=1.30

# Your Faster-Whisper transcription (this is what you actually use)
faster-whisper==1.0.3
torch>=2.0.0,<2.3.0          # Required by faster-whisper, safe version

# Audio recording (for Streamlit)
sounddevice
soundfile
numpy

# TTS
gtts

# PDF
reportlab

# Your agent & other dependencies
sentence-transformers==2.2.2
transformers==4.35.0
nltk==3.8.1
requests==2.31.0
python-multipart==0.0.6
pydantic

# Optional (you can keep them if you use them elsewhere)
firebase-admin==6.2.0
python-dotenv==1.0.0
textblob==0.17.1
langchain==0.0.346
chromadb==0.4.15